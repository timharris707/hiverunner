fund=false
package-lock=true
strict-ssl=true
min-release-age=3

# Do not enable ignore-scripts globally without a clean-install validation pass.
# HiveRunner currently depends on native packages with install scripts, including
# better-sqlite3 and sharp.
